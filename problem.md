# 故障排除编年史 (Problem Log)

> 这里记录了为了修好某个 Bug 熬过的夜，以及那些“明明代码是对的但就是跑不起来”的神奇时刻。

## 1. 初始环境搭建 (2026-04-22)
- **现象**: Spring Boot 4.0.5 版本较新，注意依赖兼容性。
- **原因**: 实验性版本。
- **方案**: 保持 pom.xml 精简，优先使用基础依赖。

## 2. CSS 文字渐变兼容性警告 (2026-04-22)
- **现象**: IDE 提示“还定义标准属性 background-clip 以实现兼容性”，部分非 Webkit 内核浏览器可能失效。
- **原因**: 仅使用了 `-webkit-` 前缀，未定义标准 `background-clip: text`。
- **方案**: 补全标准属性，且定义顺序需遵循“先私有前缀，后标准属性”原则，确保标准属性具有最高优先级。

## 3. 手机模式下 3D 地球不可见 (2026-04-22)
- **现象**: 切换到手机模式后，展示页首屏的 3D 地球消失，直接显示下方的列表卡片。
- **原因**: 
  1. **Flex 居中陷阱**: `#showcase-page` 使用 `align-items: center`，导致长内容顶部被推到视口外。
  2. **强制样式覆盖**: 末尾样式的 `!important` 禁用了响应式字体，导致超大标题横向撑爆布局。
  3. **固定位移失灵**: 地球下沉高度在短屏手机上过大，掉出视野。
- **方案**: 
  1. 父容器改为 `align-items: flex-start`。
  2. 使用 `clamp()` 实现响应式字体并允许折行。
  3. JS 动态判断 `isMobile` 调整地球 `position.y` 并触发 `resize` 事件。
