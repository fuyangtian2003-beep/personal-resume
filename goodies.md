# Starship Defender: Epic Tier Evolution (2026-04-22)

### 4. Parallax Starfield (星海视差)
- **实现**：采用 `Star` 类分层管理 150+ 粒子，修复了初始化时画布宽度未捕捉导致的左侧扎堆 Bug，现在全屏均匀分布。

### 5. Elite Enemy AI & Bullet (敌机弹幕反击)
- **设计**：`XSS`, `CRASH`, `DDOS` 标签敌机升级为精英怪，具备随机发射干扰弹的能力。

### 6. Energy Overload (EMP 能量过载)
- **大招逻辑**：当生命值与护盾全满时拾取补给，触发全屏清场。

### 7. Overclock Mode (超频爆发)
- **触发机制**：2秒内达成 15 连击。支持 8 秒无敌、幻影残影视觉。

### 2. 阶梯化 Boss 系统 (Staged Boss Battles)
*   **触发门槛**：3,000 / 10,000 / 30,000 / 100,000 分。
*   **梯度 Boss**：
    *   `PATCH_MINER` (20HP, 1掉落)
    *   `LOGIC_EATER` (50HP, 2掉落)
    *   `SYSTEM_CRASHER` (120HP, 3掉落)
    *   `ZERO_DAY_EXPLOIT` (500HP, 10掉落, 360°扩散弹幕 + 穿透激光 + 带动画的量子跃迁 + 进场即发狂暴模式)
*   **战斗平衡**：Boss 战期间维持 1/4 频率的小怪刷新。

### 3. 核心机制 (Mechanics)
*   **星海视差引擎**：多层动态星海粒子。
*   **超频暴走 (Overclock)**：2秒内达成 15 连击，进入 8 秒自动火控模式。
*   **EMP 冲击波 (Overload)**：满状态拾取补给或击败 Boss 时释放。对普通敌人秒杀，对 Boss 造成 **20点基础 + 1%最大生命值** 的比例伤害。
*   **僚机智能双模火控 (Hybrid Fire Control)**：支持“手动共振”与“空闲自瞄”双模式。用户点击时同步火力，空闲 600ms 后自动切换为持续火力输出。
*   **僚机超频系统 (Wingman Overclocking)**：当主武器等级溢出时，能量自动转化为僚机射速加成（Lv.0 - Lv.3，最高射速 150ms/发）。若任意僚机坠毁，则由于系统过载，超频增益立即清零。
*   **动态护盾力场 (Dynamic Shield Field)**：护盾具备物理模拟动画。生成时快速扩张淡入，受击时产生高亮震荡效果（Pulse），破裂时向外炸裂。
*   **战利品物理**：低速飘落 + 边缘反弹（30秒寿命，临期闪烁）。

# 炫技空间：星际蓝图 (Planned Features)

这里记录了即将实装的顶级交互功能，按优先级依次推进。

## 1. 技能星图 (Skills Constellation)
- **状态**：待启动
- **描述**：将技术栈可视化为发光星系，节点间通过能量线连接，支持拖拽与动态展开。

## 2. 全息项目投影 (Holographic Projection)
- **状态**：待启动
- **描述**：鼠标悬停项目时，在屏幕中央产生蓝色矩阵扫描特效，动态展示项目架构。

## 3. 赛博交互终端 (The Terminal)
- **状态**：✅ 已实装 (2026-04-24)
- **描述**：内置半透明命令行窗口，支持 `whoami`, `ls` 等模拟交互，体现后端极客风。
- **技术点**：
    - **身份定制**：恢复 `bob@fythub` 经典提示符，同时将 `whoami` 映射为真实身份信息。
    - **DOM 挂载点切换**：通过点击全屏按钮触发“移形换影”逻辑，容器动态挂载至 `body` 根节点，彻底解决 CSS `transform` 导致的定位截断。
    - **交互同步**：支持全屏自适应，状态随图标实时反馈。

## 4. 成就解锁系统 (Achievement System)
- **状态**：✅ 已实装 (2026-04-23)
- **描述**：全站交互反馈系统。通过监听滚动、点击、游戏结果等行为，弹出精美的玻璃态成就勋章。
- **技术点**：
    - **非阻塞队列**：使用 `queue` 数组 and `isShowing` 状态位确保多个成就连续触发时能够顺滑地逐一弹出，不会重叠。
    - **视觉增强**：CSS `backdrop-filter` 磨砂玻璃特效 + 倾斜扫光动画 (`::before` 遮罩)。
    - **持久化**：`localStorage` 同步，确保“勋章”只在第一次达成时解锁，不骚扰回头客。
- **已挂载成就**：`WELCOME` (降临), `EXPLORER` (深度探索), `PILOT` (10杀奖励), `GLOBE_TROTTER` (地球交互)。

## 8. 图标库智能双路自动降级容灾机制 (2026-06-04)
- **设计**：针对国外 `jsdelivr` CDN 在国内被墙、而国内大厂加速源在海外访问受限的痛点，构建一套“首选国内秒开、故障自适应切换国外备用源”的极简安全降级气囊。
- **技术点**：
    - **网络级 onerror 容灾切换**：在 `index.html` 的 `<link id="remixicon-cdn">` 标签上绑定 `onerror="fallbackRemixicon()"` 监听器。默认先加载国内秒开的字节跳动高速 CDN 镜像。
    - **零延迟无缝切换**：一旦国内源发生不可抗力加载失败，行内 JS 脚本会自动拦截并瞬间将 DOM 元素的 `href` 修改为国外稳定的 jsdelivr 源，保障 100% 的国内外访问成功率，控制台不报超时红字。
    - **无侵入极简设计**：保留全站原生态的 CSS 图标类名（如 `ri-`），不污染任何 DOM 和 CSS 规则，包体积近乎零增长。

## 5. 代码雨穿梭 (Code Matrix Warp)
- **状态**：✅ 已实装 (2026-04-23)
- **描述**：背景星空升级。进入“超频模式”时触发曲率驱动，星星变为向下坠落的青色代码流。
- **技术点**：
    - **形态转换**：`Star` 类支持双模渲染，根据 `isOverclocked` 状态在“像素点”与“随机字符”间切换。
    - **动力学过渡**：引入 `warpFactor` 权重，支持进入/退出的 1 秒平滑减速与淡入淡出，亮度压低至 0.4 以保护视觉。
    - **曲率驱动**：穿梭模式下背景移动速度最高提升 12 倍，营造极速穿梭感。

## 6. 终焉之战与无尽模式 (Final Boss & Endless Mode)
- **状态**：✅ 已实装 (2026-04-23)
- **描述**：构建完整的游戏进度闭环，支持无限延展的挑战。
- **细节**：
    - **最终 Boss**：200,000 分触发终极实体 `ROOT_ENTITY` (1500 HP)。
    - **通关成就**：击败最终 Boss 解锁【代码建筑师】终极勋章，宣告掌控空间逻辑。
    - **无尽模式**：通关后开启，Boss 以 3-5 万分间隔随机出现，且生命值随场次线性增长（+100 HP/级），挑战操作极限。

## 7. “子路径部署”：让 Spring Boot 里的前端“独当一面” (2026-04-23)
### Design
在传统的 Spring Boot 项目中，前端代码通常嵌套在 `src/main/resources/static` 下。在 Cloudflare Pages 部署时，利用其 **Build output directory** 特性，直接指定该子路径为根目录。

### 好处
- **无需重构项目**：不需要为了部署而强行把前端移出 Maven 结构。
- **环境一致性**：本地开发用 Spring Boot 预览，云端生产环境用 Pages 托管，两套环境共用一套源码。
- **SEO 优化**：配合子域名（如 `resume.fythub.top`），可以极大地提升个人品牌专业度。


## 9. “全栈超导芯片”一镜到底拆解交互设计 (2026-06-05)
- **状态**：✅ 已实装 (2026-06-05)
- **描述**：个人简历中极其硬核的产品实体——“全栈量子处理器芯片”。为了达到苹果官网同级别的视觉流畅度，本设计采用**“一镜到底” (One Continuous Shot)** 渲染，结合**网页序列帧 Canvas 渲染技术**，通过滚动高度精准控制播放帧。
- **分层隐喻**：
  - **顶层：【前端交互视界层】(UI/UX Glass Overlay)**：高透折射玻璃材质，流淌极光青色（Cyan）触摸流光，代表精美界面与 Vue.js / Three.js 前端交互。
  - **中层：【后端性能超频引擎】(Java Processing Core)**：深空灰拉丝超导硅晶板，中央是高亮紫光（Violet）的 Java 21 & Spring 能量堆，搭配纳米金色光纤总线。
  - **底层：【安全数据库存储基底】(Secure Data Substrate)**：钛合金装甲板，覆盖一层霓虹金色安全加密光网，代表安全稳健的 Redis/MySQL。
- **底层技术核心与硬核防爆机制**：
  - **一镜到底 Canvas 渲染**：将一镜到底的“拆解与重组”225 帧 WebP 图集进行异步预加载。基于页面滚动比例（0.00% - 100.00%）与物理 LERP 阻尼，线性映射和滑行刷新 Canvas 渲染，保证极速正放、倒放、变速播放时，比动态控制 HTML5 视频顺滑 10 倍以上，且彻底解决编解码掉帧和黑屏抖动。
  - **Delta Time 运动标准化**：为自动播放设计了基于绝对时间差的动态步长算法（基于当前分段帧速配置 * dt），抹平高低刷新率显示器（60Hz、144Hz 及 200Hz+ 以上）下的速度差异，保证全设备下播放和匀速回溯时间恒定。
  - **交互防死锁“滚动刹车”**：由于自动播放通过 scrollTo 触发的 scroll 事件属于浏览器异步行为，为防范变量竞态死锁，直接将“手动暂停”绑定于实体输入（`wheel`、`touchmove`、`keydown`）。一旦检测到用户手势，零延迟掐死自动流程，归还完全掌控权。
  - **去帧数化发光进度条**：首屏加载隐藏具体的帧数图集数值，重塑文案为“正在静默构建芯片三维神经元映射，请稍候...”，并添加一道精美的青紫色渐变发光进度条，极大增添神秘科技感。
  - **极简赛博音频控制器 (Mute Controller)**：右下角以药丸型磨砂玻璃胶囊代替原调试看板，自带 4 柱 CSS 电平律动波形动画。点击它可一键静音（阻止事件冒泡防止触发自动播放）。
  - **音画同步与生命周期防挂死**：音频采用 `loop = true` 强健属性，彻底规避音频短且无原生循环时在后台播放完毕导致的 `ended` 挂死（无法二次启动发声）。结合动画状态机，在首屏重开或倒带到起点时由 JS 手动将 `currentTime` 重置为 0，且在到达终点时提供 1.5 秒线性音量渐隐淡出（Fade Out），重返首屏时再重置为 1.0 音量重新欢唱。
  - **调试 HUD 校准**：修复了因 renderLoop 主循环与 calculateFPS 内部双重更新 `lastFrameTime` 导致 delta 帧间隔急剧缩减（误把帧内运算时间当做两帧间隔）算出的上万 FPS 天文数字。改为单源传递，精准还原真实渲染帧率。

## 10. “按需加载 JS 容灾与国内镜像源”双路气囊机制 (2026-06-05)
- **设计**：针对 3D 物理渲染引擎 Three.js 库体积大、静态载入容易造成首屏白屏的痛点，采用 JS 动态按需载入。为了贯彻“国内极速源为主、国外备用源镜像”的规约，首度架构了动态脚本的双路容灾降级气囊。
- **技术点**：
  - **国内加速首选**：动态加载首选指向国内镜像源 `cdnjs.net`，其国内缓存节点能够实现十几毫秒 of 瞬时下载，保障 3D 地球瞬间呈现。
  - **动态 onerror 切换机制**：通过在创建的 `<script>` 上绑定 `onerror` 监听器，一旦国内镜像发生网络阻断、解析超时或宕机，JS 会捕获错误并零延迟执行 `script.remove()` 清理失效 DOM，随后重新创建一个指向国外官方源 `cdnjs.cloudflare.com` 的新标签。
  - **100% 访问畅通**：该机制实现了海内外及弱网环境下的弹性自适应，最大程度缩短等待时间，且保证无任何控制台报错污染。

## 11. 赛博微动效：基于 Lucide-Animated 的高性能 SVG 动态图标设计 (2026-06-05)
- **藏宝阁灵感**：收录 [Lucide-Animated](https://lucide-animated.com/) 与 [Lucide CN](https://lucide.nodejs.cn/icons/) 作为未来项目核心的视觉微交互增强库。
- **设计思路**：
  - **CSS 轨迹动画驱动**：直接获取 Lucide 的规范 SVG 路径，利用 CSS 属性（如 `stroke-dasharray`、`stroke-dashoffset`、`transform-origin`）在 `:hover` 或 `:active` 时触发微妙动画。例如：
    - `settings` 图标：鼠标滑过时齿轮平滑顺时针自转。
    - `mail` 图标：滑过时信封盖子向下折叠展示拆信效果。
    - `arrow` 图标：产生滑行并带淡入淡出的脉冲效果。
  - **极致性能**：动态 SVG 微动效完全由浏览器 CSS 引擎进行硬件加速（GPU 渲染），相较于 Lottie 库（上百 KB 的 JS 运行开销）或动图，体积缩减至几百字节，且支持响应式改变颜色和大小，是极客风界面的灵魂点缀。
## 12. 3D 模型离线免跨域 Base64 二进制内存渲染方案 (2026-06-06)
- **设计**：在纯静态前端且无后端环境下，浏览器在 `file:///` 协议下会强行拦截对 `.glb` 3D 模型的 Fetch 跨域请求；若模型经过 Draco 压缩，其 WASM 解码文件的读取还会导致二次安全拦截。为此，我们推出全离线的 Base64 二进制内存解析方案。
- **技术点**：
  - **免 Draco WASM 阻断**：改用未压缩版 13.0MB 处理器原始模型，使用 PowerShell 将其编码为 Base64 并保存于 `model_data.js` 的 `CHIP_MODEL_BASE64` 变量中（约 17.3MB），彻底消除了 Draco WASM 对 CORS 跨域安全红线的触发因子。
  - **纯内存 ArrayBuffer 解析**：利用 `base64ToArrayBuffer` 算法在内存中将 Base64 数据解码为二进制 `ArrayBuffer`，并使用 `GLTFLoader.parse()` 原地解析渲染。全程 0 网络请求，双击 HTML 即可在 50ms 内瞬间秒开 3D 视界。
  - **双路本地化离线依赖**：将 `three.min.js`、`GLTFLoader.js` 及 `OrbitControls.js` 统一拉至本地，配置本地文件为 `primary` 极速加载源，CDN 作为 `fallback` 容灾源，保证完全断网无网环境下 3D 功能依旧 100% 满血可用。
  - **一镜到底滚动姿态 Lerp 联动**：将页面滚动比（0% - 100%）通过阻尼 Lerp 物理差值驱动芯片的 X/Y 轴自转与镜头高度。禁用 controls 控制器的滚轮缩放 (`enableZoom = false`)，使鼠标滚轮事件无阻碍穿透，解决 OrbitControls 独占滚轮事件导致一镜到底滚动卡死的冲突。

## 13. 双阶段虚拟帧映射与极简自问式下滑指示器 (2026-06-06)
- **设计**：在 2D WebP（0-224帧）全套拆解重组播毕后，采用“断层式”接力触发 3D 芯片天降（225-264帧）。自动 Demo 最多只播到 224 帧并淡出 BGM 停留，下面 3D 区域必须手动滚入。
- **指示器设计**：在 WebP 合体完毕时（192-224帧），底部居中亮起青色发光下滑提示器（英文：WHAT CAN I DO? / 中文：下滑继续探索）。进入 3D 阶段后，指示器和招募文字卡片自动淡出，实现纯享 3D 天降视觉与手势把玩，给未来的演示留足想象空间。

## 14. 跨项目安全 AI 代理中转与浏览器跨域隔离防线 (2026-06-06)
- **设计**：针对纯静态前端无法在浏览器中安全暴露三方 API Key 的痛点，首创利用服务器已有的独立 Java 后端作为安全代理屏障，通过系统环境变量解耦、自适应 IP 频次限流、以及 Spring 拦截器跨域白名单，实现高安全、零多余内存消耗的跨项目复用。
- **技术点**：
  - **环境变量托管与防漏**：后端配置 `${OPENROUTER_API_KEY:}`，密钥仅保存在服务器环境变量中，代码中仅保留占位符，保证代码开源至 GitHub 时的物理脱敏与绝对安全。
  - **IP 级滑动计数防盗刷**：在单机内存中利用 `ConcurrentHashMap` 结合 `CopyOnWriteArrayList` 实现 IP 每分钟不超过 5 次的限流拦截，返回 `429 TOO_MANY_REQUESTS`，防御脚本恶意盗刷。
  - **跨项目拦截器白名单放行**：通过在已有 WebMVC 配置中添加 `/api/ai/**` 放行路径，使静态简历前端跨域请求本地或公网后端时，完美穿透 `AuthInterceptor` 鉴权，达成极简优雅复用。

## 15. 3D 引擎本地化与双协议自适应地球贴图加载器 (2026-06-06)
- **技术亮点**：
  - **静态资源 100% 本地化**：首选引用本地自带的 `js/lib/three.min.js` 进行渲染引擎的初始化，废除并切断先前不稳定的第三方国内加速源，让 3D 初始化在弱网或无网环境下依然毫秒级唤醒。
  - **协议自适应加载器 (Protocol-Adaptive Loader)**：完美解决了在双击直接打开（`file://`）协议下本地图片由于浏览器安全沙箱 CORS 同源拦截导致地球黑屏、而在线部署（`http/https`）追求零外网下行流量的冲突。在 `js/main.js` 中使用 `window.location.protocol` 进行动态分流：
    - `file://` 环境：自动加载支持跨域的外网 `unpkg` 地址，彻底绕开 CORS 限制，地球瞬间呈现且控制台零报错。
    - http/https 环境：直接读取本地的高清 img/earth-blue-marble.jpg，不消耗任何外网流量，实现极速渲染。

## 16. 跨版本 Three.js 双路智能 prefetch 预温缓存 (2026-06-11)
- **藏宝阁灵感**：解决三方 3D 物理引擎版本升级带来的色彩渲染代差（如 r128 WebGLRenderer 渲染变暗，而新版 0.160.0 地球色彩明亮高保真）与老旧 3D 芯片模型版本兼容性的冲突。
- **技术点**：
  - **隔离运行**：主屏直接加载运行 0.160.0，体验舱动态载入 r128。
  - **双向预热**：在主屏头部利用 prefetch 机制，在用户空闲时静默预加载这两个版本的 ThreeJS 依赖，以及 18MB 的大芯片模型数据。有网时进入体验舱秒开并 100% 命中缓存，无网时 onerror 降级本地依赖，完美兼顾画质色彩与秒开体验。

## 17. 225 帧 WebP 图集流式分批加载控流器 (2026-06-11)
- **技术点**：
  - **信道避峰**：为防止 225 帧序列帧图集大批量并发请求瞬间占满浏览器 TCP 连接信道，导致主页关键 JS 阻塞，将加载时机后置到 window `load` 事件触发 1.5 秒后的空闲阶段。
  - **物理限流**：借助 `requestIdleCallback` 启动 `preloadWebPFrames`，以每次 10 张为一批次、每批间隔 100ms 的流式机制静默预拉取。确保在完成 100% 缓冲的同时，留出充足的信道冗余，交互体验丝滑无阻。

## 18. 响应式手风琴折叠子菜单组件 (2026-06-11)
- **技术点**：
  - **非对称交互**：解决多入口分类在小屏设备下的高度溢出痛点。
  - **响应式过渡**：利用 CSS 媒体查询，大屏（PC）隐藏折叠小箭头并保持平铺展示，免去大屏用户点击；小屏（移动端）激活默认高度为 0 的折叠状态，点击通过 `max-height` 的 transition 结合 `transform` 的箭头旋转，产生顺滑手风琴抽屉动画。
  - **事件防穿透**：在 JavaScript 中挂载针对折叠触发器的点击拦截，执行 `e.stopPropagation()` 阻止事件向上冒泡，杜绝了子菜单切换状态时外层大菜单被连带关闭的逻辑 Bug。

## 19. 跨环境 PDF 页面高清矩阵渲染提取器与优雅毛玻璃详情弹窗 (2026-06-14)
- **设计**：结合 Windows 本地 Python 3.14+ 及 Pillow，自动检查并静默安装 `PyMuPDF` 依赖，将多页 PDF 的各页面转化为 2 倍分辨率的高清 PNG，输出到项目静态目录，并使用磨砂玻璃弹窗（Glassmorphism Modal）在单页简历中实现原位高清大图和项目技术亮点的流畅展示。
- **技术点**：
  - **二倍缩放矩阵渲染**：使用 `fitz.Matrix(2, 2)` 进行 2 倍高清缩放渲染页面，相比于裸提取内嵌 Image 能够完美还原 PDF 复杂的图文混合和排版。
  - **防抖跳转与原位弹窗**：把项目详情从“破坏体验的外部跳转”重塑为“单页毛玻璃模态弹窗”。通过 `a` 标签 `href="javascript:void(0)"` 拦截默认跳转，配合 `Esc` 按键和点击遮罩层外部关闭，体验极其丝滑。
  - **性能友好与移动适配**：针对移动端禁用耗费 GPU 的 filter，大屏分栏左右排版，小屏自动折叠上下排版，保证性能与兼容性。

## 20. 详情弹窗左侧图片无边界完全撑满设计 (2026-06-14)
- **状态**：✅ 已实装 (2026-06-14)
- **描述**：为了突出小程序的原画截图效果，消除详情弹窗中传统卡片式内间距带来的突兀感，对左侧大图展示区进行了“无缝隙边缘贴合”的视觉升级。
- **技术点**：
  - **padding 物理清零与圆角接力**：将左侧容器 `.project-detail-left` 内边距设为 `0 !important`，并添加 `border-top-left-radius: 20px; border-bottom-left-radius: 20px; overflow: hidden;`，使其左边大圆角与外层弹窗大卡片完美闭合，图片在完全撑满时绝不会出现尖锐死角。
  - **容器平铺退化**：清空大图外框的投影、圆角及内边框，使容器在保留 `align-items: stretch` 的状态下占满 100% 宽度与高度。
  - **防裁切 Cover 展示**：图片改为 `object-fit: cover; object-position: top center;` 高清撑满，配合 top 对齐优先展示小程序顶部核心界面，完美适应各种比例。
  - **移动端高度自适应兜底**：在 `@media (max-width: 768px)` 同样清零 padding 并加上圆角转换，固定高度为 `300px` 撑满大图，避免图片在无 padding 状态下发生高度塌陷。

## 21. 3D 量子芯片实验室演示视频空闲预温与零延迟播放器 (2026-06-14)
- **状态**：✅ 已实装 (2026-06-14)
- **描述**：针对芯片实验室中 3MB 的演示视频如果在滑入可视区才加载会导致短暂黑色方块的痛点，构建了“首页空闲期静默预热 + 实验室零延迟直读”的预温方案。
- **技术点**：
  - **首页静默预温（Prefetch）**：在 `index.html` 的头部中注入 `<link rel="prefetch" href="assets/video/guanwangyans.mp4" as="video">`。当用户在主页看简历时，浏览器会偷偷用空闲信道把 2.9MB 的视频文件加载进本地磁盘缓存。
  - **实验室零延迟接力（Preload Auto）**：在 `chip_test.html` 中将视频元素的属性改为 `preload="auto"`。一旦用户划进实验室页面，播放器不用等待网络握手，直接从本地极速拉取缓存，达到毫秒级瞬间秒开，彻底告别播放黑屏，完美配合一镜到底 3D 轨迹。
