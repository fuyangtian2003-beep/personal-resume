# 藏宝阁 (Goodies Bag)

> 这里记录了那些令人拍案叫绝的设计灵感、丝滑的动画方案，以及任何能让项目瞬间“高级”起来的小技巧。

## 1. 极致视觉方案 (2026-04-22)
- **灵感**: 采用“电竞暗紫”与“深海蓝”作为流光渐变主色调。
- **组件**: 使用 CSS `backdrop-filter: blur(20px)` 实现高级毛玻璃效果。
- **交互**: 鼠标悬浮卡片时，通过 `transform: perspective(1000px) rotateX(...) rotateY(...)` 实现 3D 偏转感。
- **光效 (2026-04-22)**: 实现 `cursor-glow` 鼠标跟随效果，采用大尺寸径向渐变（600px -> 800px），并根据目标元素动态调整亮度和大小。

## 2. 动量粒子拖尾特效 (2026-04-22)
- **设计**: 在特定“炫技”页面实现基于鼠标瞬时位移（dx, dy）的粒子喷射效果。
- **算法**: 
  - **冲量赋予**: 计算 `mousemove` 瞬时位移作为粒子初始向量。
  - **物理仿真**: 引入摩擦力系数（0.95）实现自然减速，使用透明度线性衰减模拟粒子消失。
  - **静默边界**: 采用严格的边缘检测逻辑，粒子出界即刻销毁，保持画面纯净。
- **性能**: 使用独立的透明 Canvas 覆盖层，限制粒子数上限为 100，确保极致流畅。

## 3. 阶梯式自平衡背景星阵 (2026-04-22)
- **设计**: 实现全域随机分布的“呼吸感”背景星空。
- **算法 - 阶梯式补货系统**:
  - **饥渴模式 (< 100 颗)**: 极速产出 (5 帧/颗)，用于页面冷启动时的视觉快速填充。
  - **平稳模式 (100 - 200 颗)**: 低频维护 (40 帧/颗)，维持星空的动态平衡感。
  - **呼吸生命周期**: 采用 `phase: in/out` 逻辑，实现极慢的淡入 (alpha+=0.003) 与淡出 (alpha-=0.0015)，模拟真实星辰的幽远感。
- **价值**: 解决了静态背景太死板、全量动态背景太吃资源的矛盾，实现了“低开销、高逼格”的科幻氛围。
